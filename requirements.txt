# Core: decoding, arrays, image ops
av
numpy
opencv-python
pillow

# Visual channel (DINOv2 via transformers)
torch
torchvision
transformers

# Text channel (on-screen text)
rapidocr-onnxruntime

# Speech channel (optional at runtime; degrades gracefully if absent)
faster-whisper

# Chapter titles
anthropic

# EFS Stages 2-3 query-driven selection (cuts/select.py) — optional
# open-clip-torch
